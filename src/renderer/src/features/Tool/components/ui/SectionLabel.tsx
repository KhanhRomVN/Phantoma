export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <div className="text-[9.5px] font-bold tracking-[0.1em] text-[#3d4a61] uppercase px-1 pb-1 pt-2 select-none">
      {children}
    </div>
  )
}
