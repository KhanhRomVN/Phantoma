/**
 * ------------------------------------------------------------------
 * Handlers Hooks Index
 * ------------------------------------------------------------------
 * Re-export các hooks xử lý message và textarea.
 *
 * Main exports:
 * - useMessageHandlers  : Hook xử lý message events
 * - useTextareaHandlers : Hook xử lý textarea events
 * ------------------------------------------------------------------
 */

// ─── Exports ────────────────────────────────────────────────────────────
export { useMessageHandlers } from "./useMessageHandlers";
export { useTextareaHandlers } from "./useTextareaHandlers";
