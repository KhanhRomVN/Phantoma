/**
 * ------------------------------------------------------------------
 * Cấu hình cửa sổ
 * ------------------------------------------------------------------
 * Kích thước và tiêu đề mặc định cho cửa sổ ứng dụng chính.
 * Được sử dụng khi tạo BrowserWindow lúc khởi động.
 *
 * Hằng số chính:
 * - windowConfig : Kích thước mặc định, kích thước tối thiểu và tiêu đề
 * ------------------------------------------------------------------
 */

// ─── Constants ──────────────────────────────────────────────────────────
export const windowConfig = {
  defaultWidth: 1200,
  defaultHeight: 800,
  minWidth: 800,
  minHeight: 600,
  title: 'Phantoma',
};
