/**
 * ------------------------------------------------------------------
 * Barrel export CDP
 * ------------------------------------------------------------------
 * Tái xuất các module quản lý CDP từ thư mục features/cdp.
 *
 * Export chính:
 * - cdp-manager : Trình quản lý kết nối CDP lõi và singleton
 * ------------------------------------------------------------------
 */

export * from './cdp-manager';