/**
 * ------------------------------------------------------------------
 * Barrel export Shared
 * ------------------------------------------------------------------
 * Barrel tạm thời cho module shared. Các module trạng thái
 * chia sẻ riêng lẻ được import trực tiếp từ file của chúng.
 *
 * Export chính:
 * - (chưa có — tạm thời)
 * ------------------------------------------------------------------
 */

export {}; // Placeholder for shared exports
